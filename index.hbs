{{!< default}}

<div class="blog-listing">
    <h1 class="blog-title">Blog</h1>
    <div class="post-feed">
        {{#foreach posts}}
            {{> post-card}}
        {{/foreach}}
    </div>

    {{pagination}}
</div>
