---
phase: 04-section-pages-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - page-publications.hbs
  - page-products.hbs
  - page-experiments.hbs
  - assets/css/screen.css
autonomous: true
requirements:
  - SECT-01
  - SECT-02
  - SECT-03

must_haves:
  truths:
    - "Publications page renders a list of academic papers with title, venue, year, and link"
    - "Products page renders a card grid with image, name, description, and link for each product"
    - "Experiments page renders a card grid with cards linking to external projects"
    - "All external links open in new tab with rel=noopener noreferrer"
  artifacts:
    - path: "page-publications.hbs"
      provides: "Custom template for /publications/ Ghost page"
      contains: "publications-list"
    - path: "page-products.hbs"
      provides: "Custom template for /products/ Ghost page"
      contains: "card-grid"
    - path: "page-experiments.hbs"
      provides: "Custom template for /experiments/ Ghost page"
      contains: "card-grid"
    - path: "assets/css/screen.css"
      provides: "Card grid and publication list styles"
      contains: "publication-item"
  key_links:
    - from: "page-publications.hbs"
      to: "default.hbs"
      via: "{{!< default}} layout inheritance"
      pattern: "\\{\\{!< default\\}\\}"
    - from: "page-products.hbs"
      to: "default.hbs"
      via: "{{!< default}} layout inheritance"
      pattern: "\\{\\{!< default\\}\\}"
    - from: "page-experiments.hbs"
      to: "default.hbs"
      via: "{{!< default}} layout inheritance"
      pattern: "\\{\\{!< default\\}\\}"
---

<objective>
Create the three curated section page templates — Publications, Products, and Experiments — each as a Ghost `page-{slug}.hbs` custom template with corresponding CSS styles.

Purpose: These are the core content showcase pages that display Jess's academic work, products, and experiments. Each uses hardcoded placeholder content that Jess will replace with real entries.
Output: Three new .hbs template files and CSS additions to screen.css.
</objective>

<execution_context>
@/Users/jessholbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessholbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-section-pages-and-polish/04-RESEARCH.md

<interfaces>
<!-- Existing layout and design system the executor needs -->

From default.hbs: All page templates use `{{!< default}}` to inherit the sidebar layout.

From assets/css/screen.css (design tokens):
```css
:root {
    --color-bg: #FAF7F2;
    --color-text: #2D2926;
    --color-accent: #B5594B;
    --color-accent-hover: #9E4A3C;
    --color-link: var(--color-accent);
    --color-border: #E8E0D5;
    --color-muted: #7A6E65;
    --font-heading: 'Lora', Georgia, serif;
    --font-body: 'IBM Plex Serif', Georgia, serif;
    --space-1: 0.4rem; --space-2: 0.8rem; --space-3: 1.2rem;
    --space-4: 1.6rem; --space-6: 2.4rem; --space-8: 3.2rem;
    --space-12: 4.8rem; --space-16: 6.4rem;
    --content-max: 680px;
}
```

From page.hbs (existing generic page template — section pages override this):
```handlebars
{{!< default}}
<main class="site-main">
    <article class="page {{post_class}}">...</article>
</main>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Publications, Products, and Experiments page templates</name>
  <files>page-publications.hbs, page-products.hbs, page-experiments.hbs</files>
  <action>
Create three Ghost custom page templates in the theme root directory:

**page-publications.hbs** (SECT-01):
- Use `{{!< default}}` layout inheritance
- Wrap in `<main class="site-main">` to match existing page structure
- Add `<div class="publications-page">` container
- Page title: `<h1 class="page-title">Publications</h1>`
- Add an intro paragraph: `<p class="page-intro">Selected academic publications.</p>`
- Create `<ul class="publications-list">` with 3 placeholder `<li class="publication-item">` entries
- Each item has: `<a class="publication-title" href="#" target="_blank" rel="noopener noreferrer">Paper Title</a>` and `<span class="publication-meta">Venue Name &middot; 2024</span>`
- Add HTML comment `<!-- Replace placeholder entries with real publications -->` above the list

**page-products.hbs** (SECT-02):
- Use `{{!< default}}` layout inheritance
- Wrap in `<main class="site-main">`
- Add `<div class="products-page">` container
- Page title: `<h1 class="page-title">Products</h1>`
- Intro: `<p class="page-intro">Products I've built or contributed to.</p>`
- Create `<div class="card-grid">` with 2 placeholder `<article class="product-card">` entries
- Each card has: image link wrapping `<img class="product-card-image" src="https://placehold.co/400x240?text=Product" alt="Product Name" loading="lazy">`, a `<div class="product-card-body">` with `<h2 class="product-card-name">` containing a linked title, and `<p class="product-card-desc">` with placeholder description
- All external links: `target="_blank" rel="noopener noreferrer"`
- Add HTML comment `<!-- Replace placeholder cards with real products -->`

**page-experiments.hbs** (SECT-03):
- Use `{{!< default}}` layout inheritance
- Wrap in `<main class="site-main">`
- Add `<div class="experiments-page">` container
- Page title: `<h1 class="page-title">Experiments</h1>`
- Intro: `<p class="page-intro">Web experiments and side projects.</p>`
- Create `<div class="card-grid">` with 2 placeholder `<article class="experiment-card">` entries
- Same card structure as products but with class `experiment-card` instead of `product-card`, and matching child class names (`experiment-card-image`, `experiment-card-name`, `experiment-card-desc`)
- All external links: `target="_blank" rel="noopener noreferrer"`
- Add HTML comment `<!-- Replace placeholder cards with real experiments -->`
  </action>
  <verify>
    <automated>test -f page-publications.hbs && test -f page-products.hbs && test -f page-experiments.hbs && grep -q "publications-list" page-publications.hbs && grep -q "card-grid" page-products.hbs && grep -q "card-grid" page-experiments.hbs && echo "PASS"</automated>
  </verify>
  <done>All three template files exist in theme root, each extends default layout, contains properly structured placeholder content, and all external links have target="_blank" rel="noopener noreferrer"</done>
</task>

<task type="auto">
  <name>Task 2: Add section page CSS styles to screen.css</name>
  <files>assets/css/screen.css</files>
  <action>
Append the following CSS sections to the end of `assets/css/screen.css` (before any existing dark mode section — actually, append after the dark mode section since these styles use CSS variables that work in both modes):

**Section Page Common Styles:**
```css
/* Section Pages — Common */
.page-intro {
    color: var(--color-muted);
    font-size: 1.5rem;
    margin-bottom: var(--space-8);
}
```

**Publications List (SECT-01):**
```css
/* Publications */
.publications-page {
    max-width: var(--content-max);
}

.publications-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
}

.publication-item {
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
}

.publication-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
}

.publication-title {
    font-family: var(--font-heading);
    font-size: 1.8rem;
    font-weight: 600;
    display: block;
    margin-bottom: var(--space-1);
}

.publication-meta {
    font-size: 1.3rem;
    color: var(--color-muted);
}
```

**Card Grid (shared by Products SECT-02 and Experiments SECT-03):**
```css
/* Card Grid — Products & Experiments */
.products-page,
.experiments-page {
    max-width: 860px;
}

.card-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
}

.product-card,
.experiment-card {
    border: 1px solid var(--color-border);
    border-radius: 4px;
    overflow: hidden;
    transition: box-shadow 150ms ease;
}

.product-card:hover,
.experiment-card:hover {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.product-card-image,
.experiment-card-image {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
}

.product-card-body,
.experiment-card-body {
    padding: var(--space-4);
}

.product-card-name,
.experiment-card-name {
    font-size: 1.8rem;
    margin-bottom: var(--space-1);
}

.product-card-name a,
.experiment-card-name a {
    color: var(--color-text);
    text-decoration: none;
}

.product-card-name a:hover,
.experiment-card-name a:hover {
    text-decoration: underline;
}

.product-card-desc,
.experiment-card-desc {
    font-size: 1.4rem;
    color: var(--color-muted);
    margin-bottom: 0;
}

.product-card-image-link,
.experiment-card-image-link {
    display: block;
}

@media (max-width: 768px) {
    .card-grid {
        grid-template-columns: 1fr;
    }
}
```

Place all new CSS after the existing dark mode / theme toggle section (after line ~486). The styles use CSS variables so they automatically work in both light and dark mode.

Run `npx gscan .` after adding styles to verify theme still passes validation.
  </action>
  <verify>
    <automated>cd /Users/jessholbrook/Desktop/general-purpose && grep -q "publications-list" assets/css/screen.css && grep -q "card-grid" assets/css/screen.css && grep -q "publication-meta" assets/css/screen.css && npx gscan . 2>&1 | tail -5</automated>
  </verify>
  <done>screen.css contains all section page styles (publications list, card grid, product/experiment cards), responsive mobile breakpoint collapses grid to single column, and gscan validation passes</done>
</task>

</tasks>

<verification>
- `npx gscan .` passes with zero fatal errors
- All three template files exist and contain `{{!< default}}` layout inheritance
- All external links in templates have `target="_blank" rel="noopener noreferrer"`
- CSS uses existing design tokens (no hardcoded colors/spacing)
- Card grid is responsive (2 columns desktop, 1 column mobile)
</verification>

<success_criteria>
- page-publications.hbs renders a publication list with title, venue, year, link per entry
- page-products.hbs renders a 2-column card grid with image, name, description, link
- page-experiments.hbs renders a 2-column card grid with cards linking externally
- All CSS uses existing design tokens from :root
- gscan validation passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-section-pages-and-polish/04-01-SUMMARY.md`
</output>
