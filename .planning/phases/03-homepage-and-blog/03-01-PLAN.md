---
phase: 03-homepage-and-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - home.hbs
  - routes.yaml
  - assets/css/screen.css
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "Homepage displays a bold serif hero with the intro statement"
    - "Section preview links appear below the hero (Blog, Publications, Products, Experiments, About)"
    - "routes.yaml exists and maps / to home.hbs and /blog/ to index.hbs"
  artifacts:
    - path: "home.hbs"
      provides: "Bold hero homepage template"
      contains: "hero-title"
    - path: "routes.yaml"
      provides: "Ghost routing config for custom homepage"
      contains: "/blog/"
    - path: "assets/css/screen.css"
      provides: "Hero and section preview styles"
      contains: "home-hero"
  key_links:
    - from: "routes.yaml"
      to: "home.hbs"
      via: "routes / maps to home template"
      pattern: "/: home"
    - from: "home.hbs"
      to: "default.hbs"
      via: "template inheritance"
      pattern: "{{!< default}}"
---

<objective>
Create the bold serif hero homepage (home.hbs) with section preview links and the routes.yaml file that tells Ghost to serve it at `/`.

Purpose: The homepage is the first impression — a personality-forward intro in the style of Maggie Appleton's bold serif hero, with warm tones and generous whitespace established in Phase 2.

Output: home.hbs template, routes.yaml for Ghost Admin upload, hero + section preview CSS in screen.css.
</objective>

<execution_context>
@/Users/jessholbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessholbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-and-blog/03-RESEARCH.md

<interfaces>
<!-- Existing design system tokens and layout from Phase 2 -->

From assets/css/screen.css:
```css
:root {
    --color-bg: #FAF7F2;
    --color-text: #2D2926;
    --color-accent: #B5594B;
    --color-accent-hover: #9E4A3C;
    --color-border: #E8E0D5;
    --color-muted: #7A6E65;
    --font-heading: 'Lora', Georgia, serif;
    --font-body: 'IBM Plex Serif', Georgia, serif;
    --space-4: 1.6rem;
    --space-6: 2.4rem;
    --space-8: 3.2rem;
    --space-12: 4.8rem;
    --space-16: 6.4rem;
    --content-max: 680px;
}
```

From default.hbs:
```handlebars
<!-- site-frame grid with sidebar + main, body outputs {{{body}}} inside .site-main -->
<div class="site-frame">
    <aside class="site-sidebar">...</aside>
    <main class="site-main">{{{body}}}</main>
</div>
```

Note: Templates extend default.hbs via `{{!< default}}`. The `{{{body}}}` placeholder in default.hbs is INSIDE `<main class="site-main">`, so home.hbs content should NOT wrap in another `<main>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create home.hbs hero template and routes.yaml</name>
  <files>home.hbs, routes.yaml</files>
  <action>
Create `home.hbs` extending default.hbs with:
1. A `<div class="home-hero">` containing an `<h1 class="hero-title">` with text: "Jess writes about people, AI, and people + AI."
2. Below the hero, a `<div class="home-sections">` with section preview cards. Each card is a `<div class="section-preview">` containing:
   - `<h2 class="section-title">` wrapping an `<a>` linking to the section URL
   - `<p class="section-desc">` with a one-line description
   Sections to include:
   - Blog (/blog/) — "Writing on the intersection of people and AI."
   - Publications (/publications/) — "Academic papers and research."
   - Products (/products/) — "Things built and contributed to."
   - Experiments (/experiments/) — "Web-based projects and prototypes."
   - About (/about/) — "Background, bio, and what drives this work."

The template starts with `{{!< default}}` (NOT wrapped in `<main>` — default.hbs already provides that).

Create `routes.yaml` at project root (NOT in the theme zip — this is uploaded separately to Ghost Admin):
```yaml
routes:
  /: home

collections:
  /blog/:
    permalink: /blog/{slug}/
    template: index
```

IMPORTANT: routes.yaml is uploaded to Ghost Admin (Settings > Labs > Routes), not bundled in the theme zip. The zip script already only includes `*.hbs partials/ assets/ package.json`.
  </action>
  <verify>
    <automated>test -f home.hbs && grep -q "hero-title" home.hbs && grep -q "home-sections" home.hbs && test -f routes.yaml && grep -q "/blog/" routes.yaml && npx gscan . 2>&1 | head -5</automated>
  </verify>
  <done>home.hbs exists with hero title and 5 section preview links; routes.yaml exists with / mapped to home and /blog/ collection; gscan passes</done>
</task>

<task type="auto">
  <name>Task 2: Add homepage hero and section preview CSS</name>
  <files>assets/css/screen.css</files>
  <action>
Append homepage styles to `assets/css/screen.css` (BEFORE the dark mode section, after the Ghost editor width classes):

1. `.home-hero` — generous top padding (var(--space-16)), bottom margin (var(--space-12)), max-width var(--content-max).
2. `.hero-title` — font-family var(--font-heading), font-size 4rem (large and bold), font-weight 600, line-height 1.2, color var(--color-text). On mobile (max-width 768px), scale to 2.8rem.
3. `.home-sections` — display grid, 1 column, gap var(--space-6).
4. `.section-preview` — padding-bottom var(--space-6), border-bottom 1px solid var(--color-border). Last child: no border.
5. `.section-title` — font-size 2rem, margin-bottom var(--space-1). The `a` inside should use color var(--color-text), text-decoration none, with hover underline.
6. `.section-desc` — color var(--color-muted), font-size 1.5rem, margin-bottom 0.

These styles use ONLY existing design tokens — no new hex values or custom properties needed.
  </action>
  <verify>
    <automated>grep -q "home-hero" assets/css/screen.css && grep -q "hero-title" assets/css/screen.css && grep -q "section-preview" assets/css/screen.css && npm run build</automated>
  </verify>
  <done>Homepage hero renders with large serif title, section previews display as a clean vertical list with descriptions and dividing borders, build succeeds</done>
</task>

</tasks>

<verification>
- `npx gscan .` passes with zero fatal errors
- `home.hbs` exists and contains hero markup with the exact intro statement
- `routes.yaml` exists at project root with correct routing config
- `npm run build` succeeds (CSS compiles)
- Hero uses only design tokens from Phase 2 (no hardcoded colors)
</verification>

<success_criteria>
- home.hbs extends default.hbs and displays "Jess writes about people, AI, and people + AI." in a bold serif hero
- Five section preview links (Blog, Publications, Products, Experiments, About) appear below the hero
- routes.yaml maps / to home and /blog/ to index with /blog/{slug}/ permalinks
- All CSS uses existing design tokens; no new custom properties needed
- gscan validation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-and-blog/03-01-SUMMARY.md`
</output>
